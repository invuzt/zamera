[package]
name = "zamera_core"
version = "0.1.0"
edition = "2021"

[lib]
name = "zamera_core"
crate-type = ["staticlib"] # Kita buat static agar menyatu dengan C

[dependencies]
# Tanpa dependencies berat dulu agar build cepat
